syntax = "proto3";

service Greeter {
  // Sends a greeting
  rpc SayHello (Listing) returns (Listing) {}
}

// The request message containing the user's name.
message Listing {
  string address = 1;
  string title = 2;
  string cost = 3;
}
